import React from "react";

export default props => {
  return (
    <div>
      <h1>
        There doesn't seem to be any boards for this Location. Please create one
        by pressing the + at the bottom of the boards bar
      </h1>
    </div>
  );
};
